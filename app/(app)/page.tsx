import { Page, PageHeader } from "@/components/page-shell";

// Placeholder. The expiry dashboard replaces this in the next phase.
export default function HomePage() {
  return (
    <Page>
      <PageHeader
        title="Today"
        subtitle="The expiry alert panel lands here."
      />
    </Page>
  );
}
